---
"@deltic/transaction-manager": minor
---

Initial release. Defines the `TransactionManager` interface for managing database transactions with `begin()`, `commit()`, `rollback()`, `inTransaction()`, and `runInTransaction()`.
